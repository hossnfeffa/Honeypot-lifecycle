# Honeypot-lifecycle
An end-to-end honeypot exercise: deploy an intentionally exposed host, capture attacker activity via MDE Live Response, hunt it with KQL (Advanced Hunting + Sentinel), and report findings — including a geo-mapped Azure Workbook of inbound auth attempts
